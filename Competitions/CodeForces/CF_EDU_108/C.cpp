# include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve(){
    int n;
    int temp;
    cin >> n;
    unordered_map <int, vector <ll> > mp;
    mp.max_load_factor(0.25);

    for(int i = 0; i < n; ++i){
        cin >> temp;
        if(!mp[temp]) 

    }
    
}
int main(){
    int t;
    cin >> t;
    while(t--) solve();
    return 0;
}